[
  {
    "resource_user": "root",
    "resource": {
      "module": "users",
      "client": "${resource_client.global}",
      "root": true,
      "fields": {
        "email": "dev@example.com",
        "password": "${env('ROOT_PASSWORD', 'secret')}",
        "name": "Löwe Root Account"
      }
    }
  },
  {
    "resource_user": "admin",
    "depends_on": "resource_entity.role_admin",
    "resource": {
      "module": "users",
      "client": "${resource_client.global}",
      "fields": {
        "email": "admin@example.com",
        "password": "${env('ADMIN_PASSWORD', 'secret')}",
        "name": "Löwe Admin Account",
        "roles": [
          "${resource_entity.role_admin}"
        ]
      }
    }
  }
]
