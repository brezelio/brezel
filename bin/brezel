#!/usr/bin/env bash

set -euo pipefail

script_dir="$(cd -- "$(dirname -- "${BASH_SOURCE[0]}")" && pwd)"

tty_args=()
if [ ! -t 0 ] || [ ! -t 1 ]; then
  tty_args=(-T)
fi

exec "$script_dir/compose" exec "${tty_args[@]}" app php "$@"
